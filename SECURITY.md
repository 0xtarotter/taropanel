# 安全说明

这是用于可信管理员的高权限主机面板，不是共享主机的租户隔离系统。公网必须使用 HTTPS，建议限制来源 IP 或放在 VPN 后。

请勿在 Issue 中提交配置文件、SSH 私钥、环境变量、Compose 密码、会话 cookie 或实际终端输出。报告漏洞时先描述影响和最小复现；不要公开可利用的凭据。可使用 GitHub 仓库提供的私密漏洞报告功能（若已启用）。

仓库与发布包禁止包含 `config.json`、`sessions.json`、`data/`、`.env`、SSH 密钥、生产截图及日志。构建使用明确文件清单，不直接打包运行目录。
